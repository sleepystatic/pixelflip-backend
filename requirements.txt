# Web Framework & Server
Flask==3.0.0
flask-cors==6.0.1
gunicorn==21.2.0
Werkzeug==3.0.0

# Database
psycopg2-binary==2.9.11
SQLAlchemy>=2.0.0
Flask-SQLAlchemy>=3.0.0

# Scraper & Automation
playwright==1.61.0
# patchright is an undetected Playwright fork, used ONLY for Mercari: it is the
# only client that gets past Mercari's Cloudflare + /v1/api 403. Keep its version
# aligned with playwright above.
patchright==1.61.2
beautifulsoup4==4.14.3
requests==2.32.5

# Payments & Logic
stripe==8.0.0
python-dotenv==1.0.1

# Async & Networking
trio==0.27.0
trio-websocket==0.11.1
websocket-client==1.8.0

# Push Notifications
pywebpush==2.0.0